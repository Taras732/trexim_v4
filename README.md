# Trexim v4 - AI Logistics Operating System

Перша AI операційна система для логістики в Україні. Автоматизація 70% рутинних операцій з готовністю до Е-ТТН 2026.

## 🚀 Технології

- **Backend:** FastAPI + Python 3.11
- **Templating:** Jinja2
- **Styling:** Tailwind CSS (CDN)
- **Interactivity:** Alpine.js
- **Server:** Uvicorn / Gunicorn

## 📦 Встановлення (локально)

```bash
# Клонувати репозиторій
git clone https://github.com/Taras732/trexim_v4.git
cd trexim_v4

# Створити віртуальне середовище
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Встановити залежності
pip install -r requirements.txt

# Запустити сервер
cd app
python main.py
```

Відкрийте http://localhost:8000

## 🌐 Розгортання на Render.com

### Крок 1: Підготовка

1. Репозиторій вже готовий для Render (є `render.yaml`)
2. Переконайтесь, що зміни відправлені на GitHub:
   ```bash
   git add .
   git commit -m "Add Render deployment config"
   git push origin main
   ```

### Крок 2: Створення сервісу на Render

1. Перейдіть на [render.com](https://render.com)
2. Зареєструйтесь/увійдіть через GitHub
3. Натисніть **"New +"** → **"Web Service"**
4. Підключіть репозиторій `Taras732/trexim_v4`
5. Render автоматично виявить `render.yaml` та налаштує все
6. Натисніть **"Create Web Service"**

### Крок 3: Підключення домену trexim.ai

1. В дашборді вашого сервісу на Render перейдіть до **"Settings"**
2. Прокрутіть до секції **"Custom Domain"**
3. Натисніть **"Add Custom Domain"**
4. Введіть `trexim.ai`
5. Render покаже DNS записи, які потрібно додати

### Крок 4: Налаштування DNS

У вашого реєстратора доменів (де куплений `trexim.ai`) додайте DNS записи:

```
Type: CNAME
Name: www
Value: <your-app>.onrender.com

Type: A (або ALIAS якщо підтримується)
Name: @
Value: <IP-адреса з Render>
```

**Або використайте Cloudflare (рекомендовано):**

1. Додайте `trexim.ai` до Cloudflare
2. Змініть nameservers у реєстратора на Cloudflare nameservers
3. В Cloudflare DNS додайте:
   - `CNAME` запис: `www` → `<your-app>.onrender.com`
   - `CNAME` запис: `@` → `<your-app>.onrender.com`

### Крок 5: SSL Сертифікат

Render автоматично створить безкоштовний SSL сертифікат для вашого домену. Зазвичай це займає 5-10 хвилин після додавання DNS записів.

## 📁 Структура проекту

```
trexim_v4/
├── app/
│   ├── templates/
│   │   ├── base.html           # Базовий шаблон з header/footer
│   │   ├── admin/              # Адмін панель
│   │   └── pages/              # Сторінки сайту
│   │       ├── home.html       # Головна
│   │       ├── about.html      # Про нас
│   │       ├── partners.html   # Партнерам
│   │       ├── pricing.html    # Тарифи
│   │       ├── tools.html      # Інструменти
│   │       ├── blog.html       # Блог
│   │       └── contact.html    # Контакти
│   ├── routes/
│   │   ├── pages.py           # Маршрути сторінок
│   │   └── admin.py           # Маршрути адміна
│   ├── config.py              # Налаштування
│   └── main.py                # Точка входу
├── requirements.txt           # Python залежності
├── render.yaml               # Конфігурація Render
├── .gitignore
└── README.md
```

## 🎨 Особливості

- ✅ Білінгвальність (Українська/Англійська)
- ✅ Responsive дизайн
- ✅ Темна тема з помаранчевими акцентами
- ✅ Інтерактивні калькулятори
- ✅ Адмін панель з автентифікацією
- ✅ Блог з фільтрами категорій
- ✅ Готовність до Е-ТТН 2026

## 🔐 Адмін панель

Доступ: `/admin`

Пароль за замовчуванням: `trexim2026` (змініть в `app/config.py`)

## 🌍 Оточення

**Development:**
```bash
cd app
python main.py
```

**Production (Render):**
Автоматично використовує Gunicorn з Uvicorn workers

## 📝 Ліцензія

© 2026 Trexim. Всі права захищені.

## 🤝 Підтримка

Email: trexim.logistics@gmail.com
Телефон: +380 99 009 1802
Адреса: Таможенна пл., 1А, Одеса, 65000
